package com.example.fhirpath.typing;

import javax.annotation.Nonnull;

public enum Type {
    INTEGER,
    DECIMAL,
    QUANTITY,
    DATE,
    DATE_TIME,
    BOOLEAN,
    STRING,
    NULL,
    UNKNOWN

}

